export interface {{ENTITY_NAME}} {
  id: number;
  name: string;
  description?: string;
  status: 'active' | 'inactive' | 'pending';
  createdAt: string;
  updatedAt: string;
}

export interface {{ENTITY_NAME}}FormData {
  name: string;
  description?: string;
  status: 'active' | 'inactive' | 'pending';
}

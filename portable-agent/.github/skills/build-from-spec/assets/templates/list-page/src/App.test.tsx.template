import { render, screen } from '@testing-library/react';
import App from './App';

describe('App', () => {
  it('renders the generated list page shell', async () => {
    render(<App />);
    expect(await screen.findByText('{{TITLE}}')).toBeInTheDocument();
  });
});
